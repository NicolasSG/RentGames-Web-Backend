spring.application.name=rentgames-web-backend
server.port=8080

# As credenciais reais do banco ficam fora do Spring Environment de proposito:
# com.rentgames.config.DatabaseConfig (reaproveitada da Etapa 6) le
# db.properties / variaveis de ambiente separadamente, para que o mesmo
# arquivo de configuracao sirva tanto o app desktop quanto o back-end web.
# Ver db.properties.example.
